import React from 'react'
import styles from '@/styles'

const CoursesMain = () => {
  return (
    <div>
        <h1 className={`${styles.heading}`}>Cursos destacados</h1>
    </div>
  )
}

export default CoursesMain